pipeline {
    agent any   // se ejecuta en cualquier agente disponible

    stages {

     stage('Instalando Docker-cli'){
          steps{
                script {
                    sh 'apt-get install -y docker.io'
                }
          }
       }

        stage('Checkout') {
            steps {
                // Clona el repositorio desde GitHub
                git branch: 'main',
                    url: 'https://github.com/francoprieto/lab.git'
            }
        }

       stage('Run Nginx') {
            steps {
                script {

                    sh '''
                        DOCKER_HOST=tcp://docker:2375 \
                        DOCKER_TLS_VERIFY= \
                        DOCKER_CERT_PATH= \
                        DOCKER_HOST=tcp://docker:2375 \
                        docker rm -f pipeline-nginx || true
                    '''

                    sh '''
                        DOCKER_HOST=tcp://docker:2375 \
                        DOCKER_TLS_VERIFY= \
                        DOCKER_CERT_PATH= \
                        docker run -d --name pipeline-nginx -p 9999:80 nginx:alpine
                    '''

                    sh '''
                        DOCKER_HOST=tcp://docker:2375 \
                        DOCKER_TLS_VERIFY= \
                        DOCKER_CERT_PATH= \
                        docker cp  $WORKSPACE/semana7/test/ pipeline-nginx:/usr/share/nginx/html/test
                    '''

                }
            }
        }
    }
}
